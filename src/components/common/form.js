import { View, StyleSheet, Text, TextInput as Input } from 'react-native';


const form = StyleSheet.create({
  form: {
    // flex: 2,
    width: '100%',
    // height: '100%',
    marginTop : 20,
    // marginVertical: 50,
    // marginLeft : -10,
    // marginHorizontal: -50,
    // marginBottom : -340,
    // flexGrow: 1
  }
});

export {form};
