import React from 'react';
import { View, StyleSheet, Text, TextInput as Input } from 'react-native';

const Codi = StyleSheet.create({
    container : {
        backgroundColor: 'white',
    },
    selectToggle : {
        borderWidth: 1,
        borderColor: '#6a6e78',
        borderRadius: 10,
        paddingVertical: 10,
        paddingHorizontal: 5,
        // marginBottom: 5,
        marginLeft: 5,
        marginTop: 15,
        width : '97%',
        height: 50,
        marginBottom: 15,
    },
});

export {Codi};
